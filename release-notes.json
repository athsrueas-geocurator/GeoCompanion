{
  "title": "Cache status in Preferences",
  "changes": [
    "Cache progress and results now appear in Preferences instead of floating over every page.",
    "Background caching continues when the progress window is closed."
  ]
}
